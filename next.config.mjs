/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized:true,
        domains: [
          "images.unsplash.com",
          "plus.unsplash.com",
          "i.ytimg.com",
        ],
      },
};

export default nextConfig;
